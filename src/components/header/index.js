import { Box, Text } from "@chakra-ui/layout";

export function Header () {
    return (
        <Box pt={8} px={4}>
            <Text fontSize="4xl" fontWeight={'bold'}>tasked</Text>
        </Box>
    )
};
